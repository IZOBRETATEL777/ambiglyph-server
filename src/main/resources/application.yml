jwt:
  secret-key: MY_SECRET_KEY

server:
  port: 8081
  servlet:
    context-path:
      /ambiglyph-server

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ambiglyph
    username: root
    password: 12345678
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
#    show-sql: true
    hibernate:
      ddl-auto: update
#debug: true
